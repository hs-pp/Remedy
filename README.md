# Remedy

NOT READY to be moved into a Unity Package.

Things to do before it's ready:
1. Remedy Config cannot be a SO inside the plugin itself because it needs to be editable by the user. 
   1. This creates a weird challenge with the source gen that needs to be solved.
2. Need to add the Remedy Console.